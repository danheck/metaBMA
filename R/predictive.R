# @param meta model-averaged meta-analysis (\code{\link{meta_bma}} or \code{\link{bma}})
# @param SE standard error for future study
# @param n number of future studies (currently, only \code{n = 1} supported)
predictive <- function(meta,
                       SE,
                       n = 1){

  # sample parameters + study effect size(s)

  # compute new Bayes factor


  # summarize / collect results

}
